# AI Agent Instructions

This repository contains a Jules PWA wrapper. Agents should follow the global Jules notification protocols, using the `ntfy_topic` and `app_url` defined in `notifications.json` for this specific project.
